# This workflow will install Python dependencies, run tests and lint with a variety of Python versions
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python

name: Python package

on:
  push:
    branches: ['main', '2024-05-21-ci-pipeline'] # FIXME, branches
  pull_request:
    branches: ['main']

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.9', '3.10', '3.11']

    steps:
      - uses: actions/checkout@v4
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install pytest
          pip install sedaro
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
      - name: Create config.py
        run: |
          echo "HOST = 'https://api.astage.sedaro.com'" >> tests/config.py
          echo "API_KEY = 'NXR00peRmTlUus5bHEbm-.emj76yc49ZYdfvPWuAkJPQKF8hzwC2RJi3zkN4hggvhvHg5qeS5wVPPkx4zUE340JltXUrJSM3z8HPywcsUxRA'" >> tests/config.py
          echo "AUTH_HANDLE = ':/'" >> tests/config.py

          echo "SIMPLESAT_A_T_ID = '/'" >> tests/config.py
          echo "SIMPLESAT_SCENARIO_ID = '/'" >> tests/config.py
          echo "WILDFIRE_A_T_ID = 'PMZrzP6XdMx9hDYWLMxdQ4'" >> tests/config.py
          echo "WILDFIRE_SCENARIO_ID = 'PMZrzPc6FSHCsNRDKBlzbM'" >> tests/config.py

      # - name: Install dependencies
      #   run: |
      #     python -m pip install --upgrade pip
      #     python -m pip install flake8 pytest
      #     if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
      # - name: Lint with flake8
      #   run: |
      #     # stop the build if there are Python syntax errors or undefined names
      #     flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
      #     # exit-zero treats all errors as warnings. The GitHub editor is 127 chars wide
      #     flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
      - name: Test with pytest
        run: |
          pytest
